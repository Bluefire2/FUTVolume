# This file contains the functions that fetch the current trade volume for a card.


def current_volume(card_id: int) -> int:
    """
    This function returns the current trade volume of a card, which is here defined as the number of cards that are on
    sale at the time of the query.

    @rtype: int
    @param card_id: The id of the card.
    @return: The current trade volume of the card.
    """
    pass
