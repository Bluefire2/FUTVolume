# This file will contain the functions that fetch the average current price of a card, as well as price ranges


def current_price(card_id: int) -> int:
    """
    Returns the current average price for the card. This is the average price that the card is currently trading for.

    @rtype: int
    @param card_id: The id of the card.
    @return: The current average price for the card.
    """
    pass


def price_range(card_id: int) -> tuple:
    """
    Returns the price range for the card.

    @rtype: tuple
    @param card_id: The id of the card.
    @return: The price range for the card, in the form (lower, upper).
    """
    pass
